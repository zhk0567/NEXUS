<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background_primary"
    android:padding="32dp">

    <!-- 顶部状态卡片 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/card_background"
        android:padding="20dp"
        android:layout_marginBottom="40dp"
        android:elevation="2dp">

        <!-- 状态指示器 -->
        <View
            android:id="@+id/status_indicator"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:background="@drawable/status_indicator"
            android:layout_marginEnd="12dp" />

        <!-- 状态文字 -->
        <TextView
            android:id="@+id/status_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="正在连接..."
            android:textSize="16sp"
            android:textColor="@color/text_primary"
            android:textStyle="bold" />

    </LinearLayout>

    <!-- 主录音区域 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">

        <!-- 三个控制按钮 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="24dp">

            <!-- 挂断按钮 -->
            <Button
                android:id="@+id/hangup_button"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:text="📞"
                android:textSize="20sp"
                android:textColor="@android:color/white"
                android:background="@drawable/button_voice_danger"
                android:elevation="4dp"
                android:layout_marginEnd="20dp" />

            <!-- 暂停/继续按钮 -->
            <Button
                android:id="@+id/pause_button"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:text="▶️"
                android:textSize="24sp"
                android:textColor="@android:color/white"
                android:background="@drawable/button_voice_primary"
                android:elevation="4dp"
                android:layout_marginHorizontal="20dp" />

            <!-- 字幕按钮 -->
            <Button
                android:id="@+id/subtitle_button"
                android:layout_width="60dp"
                android:layout_height="60dp"
                android:text="📝"
                android:textSize="20sp"
                android:textColor="@android:color/white"
                android:background="@drawable/button_voice_secondary"
                android:elevation="4dp"
                android:layout_marginStart="20dp" />

        </LinearLayout>

        <!-- 提示文字 -->
        <TextView
            android:id="@+id/hint_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="点击开始语音对话"
            android:textSize="14sp"
            android:textColor="@color/text_hint"
            android:gravity="center" />

    </LinearLayout>

    <!-- 底部日志区域 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/card_background"
        android:elevation="2dp">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp">

            <TextView
                android:id="@+id/result_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="应用已启动，等待您开始对话..."
                android:textSize="13sp"
                android:textColor="@color/text_secondary"
                android:lineSpacingExtra="4dp" />

        </ScrollView>

    </LinearLayout>

</LinearLayout>
